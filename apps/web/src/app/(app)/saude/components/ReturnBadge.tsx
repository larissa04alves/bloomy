/** Marca um item criado como retorno de outro (`parentId`), que herda o nome do original. */
export function ReturnBadge() {
  return (
    <span className="rounded-full bg-lilac-tint px-2 py-0.5 text-xs font-bold text-lilac-deep">
      retorno
    </span>
  );
}
